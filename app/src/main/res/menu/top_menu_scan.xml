<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--Action to enable/disable torch-->
    <item
        android:id="@+id/action_switch_torch"
        android:icon="@drawable/ic_flash_off_white_24dp"
        android:title="Switch torch"
        android:visible="false"
        app:showAsAction="always" />

    <!--Action to show/change the number of rows ticket being scanned has-->
    <item
        android:id="@+id/action_row_number"
        android:title="Change row number"
        android:visible="true"
        app:actionLayout="@layout/row_number_menu_item"
        app:showAsAction="always" >
    </item>

</menu>
